- name: The yourbtc.link installer
  hosts: all

  pre_tasks:
    - name: Update system
      apt:
        update_cache: yes
        upgrade: dist
      tags:
        - init

  tasks:
    - name: Install Bitcoin Core client
      import_role:
        name: bitcoin-core-role
      tags:
        - bitcoin
